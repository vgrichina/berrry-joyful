# berrry-joyful Architecture & Permission Model

## Table of Contents
1. [System Overview](#system-overview)
2. [Permission Model - How We Control Other Apps](#permission-model)
3. [Input Flow](#input-flow)
4. [Control Modes](#control-modes)
5. [Voice Input System](#voice-input-system)

---

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     berrry-joyful Application                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐        │
│  │ AppDelegate  │   │ViewController│   │StatusOverlay │        │
│  │              │──▶│              │──▶│   (HUD)      │        │
│  │ - Menu Bar   │   │ - UI         │   │              │        │
│  │ - Lifecycle  │   │ - Controller │   └──────────────┘        │
│  └──────────────┘   │   Handling   │                            │
│                     └──────┬───────┘                            │
│                            │                                     │
│         ┌──────────────────┼──────────────────┐                │
│         │                  │                  │                 │
│         ▼                  ▼                  ▼                 │
│  ┌─────────────┐   ┌──────────────┐   ┌──────────────┐        │
│  │InputControl │   │VoiceInput    │   │ControlMode   │        │
│  │   (Core)    │   │Manager       │   │ & Settings   │        │
│  │             │   │              │   │              │        │
│  │- Mouse Move │   │- Speech Recog│   │- Mode Logic  │        │
│  │- Clicks     │   │- Commands    │   │- Modifiers   │        │
│  │- Keyboard   │   │- Transcribe  │   │- Sensitivity │        │
│  └──────┬──────┘   └──────┬───────┘   └──────────────┘        │
│         │                  │                                     │
└─────────┼──────────────────┼─────────────────────────────────────┘
          │                  │
          ▼                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                  macOS System Frameworks                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ CoreGraphics    │  │   GameController│  │     Speech      │ │
│  │ (CGEvent API)   │  │    Framework    │  │   Framework     │ │
│  │                 │  │                 │  │                 │ │
│  │ Creates fake    │  │ Reads Joy-Con   │  │ Speech-to-text  │ │
│  │ mouse/keyboard  │  │ button presses  │  │ recognition     │ │
│  │ events          │  │ & stick values  │  │                 │ │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘ │
│           │                    │                     │          │
└───────────┼────────────────────┼─────────────────────┼──────────┘
            │                    │                     │
            ▼                    ▼                     ▼
    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
    │Other macOS   │    │  Joy-Con     │    │ Microphone   │
    │Applications  │    │  Controller  │    │   Input      │
    │              │    │  (Bluetooth) │    │              │
    │Browser, IDE, │    │              │    │              │
    │Terminal, etc.│    └──────────────┘    └──────────────┘
    └──────────────┘
```

---

## Permission Model - How We Control Other Apps

### The Accessibility Permission System

macOS has **strict security** to prevent apps from spying on or controlling other apps.
To control other apps, we must use the **Accessibility API** with explicit user permission.

```
┌────────────────────────────────────────────────────────────────┐
│                  Permission Request Flow                        │
└────────────────────────────────────────────────────────────────┘

Step 1: App Launches
─────────────────────
    ┌──────────────┐
    │ berrry-joyful│
    │   starts     │
    └──────┬───────┘
           │
           ▼
    Check: AXIsProcessTrustedWithOptions()
           │
           ├─── YES ──▶ Permission already granted ✓
           │
           └─── NO ───▶ Continue to Step 2


Step 2: Request Permission (InputController.swift:24-32)
────────────────────────────────────────────────────────
    ┌────────────────────────────────────────┐
    │ InputController.requestAccessibility() │
    └────────────────┬───────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────────────┐
    │  AXIsProcessTrustedWithOptions([              │
    │    kAXTrustedCheckOptionPrompt: true          │
    │  ])                                            │
    │                                                │
    │  This tells macOS: "Show permission dialog"   │
    └────────────────┬───────────────────────────────┘
                     │
                     ▼
         ┌───────────────────────────┐
         │   macOS System Dialog     │
         ├───────────────────────────┤
         │ "berrry-joyful" would     │
         │ like to control this      │
         │ computer using            │
         │ accessibility features.   │
         │                           │
         │  [ Deny ]  [ Open System  │
         │              Settings ]   │
         └─────────────┬─────────────┘
                       │
           ┌───────────┴───────────┐
           │                       │
         DENY                   OPEN SETTINGS
           │                       │
           ▼                       ▼
    App won't work       ┌──────────────────────┐
    until granted        │ System Settings >    │
                         │ Privacy & Security > │
                         │ Accessibility        │
                         │                      │
                         │ ☑ berrry-joyful     │
                         └──────────┬───────────┘
                                    │
                                    ▼
                         Permission Granted! ✓


Step 3: Creating Events (InputController.swift:100-150)
────────────────────────────────────────────────────────
    WITH PERMISSION:
    ┌────────────────────────────────────────┐
    │ let event = CGEvent(                   │
    │   mouseEventSource: nil,               │
    │   mouseType: .leftMouseDown,           │
    │   mouseCursorPosition: pos,            │
    │   mouseButton: .left                   │
    │ )                                      │
    │ event.post(tap: .cghidEventTap)       │
    └────────────────┬───────────────────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │ macOS Event System    │
         │ (CGHIDEventTap)       │
         │                       │
         │ Injects event as if   │
         │ it came from real     │
         │ hardware              │
         └───────────┬───────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │  Any Running App      │
         │                       │
         │ Receives mouse click  │
         │ or keyboard press     │
         │                       │
         │ Browser, Terminal,    │
         │ Text Editor, etc.     │
         └───────────────────────┘

    WITHOUT PERMISSION:
    ┌────────────────────────────────────────┐
    │ event.post(tap: .cghidEventTap)       │
    └────────────────┬───────────────────────┘
                     │
                     ▼
              ⚠️  BLOCKED  ⚠️
         Event is silently ignored
         Nothing happens
```

### Why This Permission Is Needed

```
┌─────────────────────────────────────────────────────────────┐
│  What Accessibility Permission Allows                        │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ✓ Create synthetic mouse events (move, click, drag)        │
│  ✓ Create synthetic keyboard events (type, shortcuts)       │
│  ✓ Post events to ANY application on the system             │
│  ✓ Move cursor anywhere on screen                           │
│  ✓ Simulate modifier keys (Cmd, Opt, Shift, Ctrl)          │
│                                                               │
│  This is POWERFUL - it's essentially remote control of       │
│  your entire Mac!                                            │
│                                                               │
│  That's why macOS requires explicit user permission.         │
└─────────────────────────────────────────────────────────────┘
```

### Other Permissions Required

```
┌──────────────────────────────────────────────────────────────┐
│  Microphone Permission (for Voice Input)                      │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│  Info.plist key: NSMicrophoneUsageDescription                 │
│                                                                │
│  Dialog: "berrry-joyful would like to access the microphone" │
│                                                                │
│  Allows: SFSpeechRecognizer to listen and transcribe speech  │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  Speech Recognition Permission                                 │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│  Info.plist key: NSSpeechRecognitionUsageDescription          │
│                                                                │
│  Dialog: "berrry-joyful would like to recognize speech"      │
│                                                                │
│  Allows: Converting speech to text for voice commands        │
└──────────────────────────────────────────────────────────────┘
```

---

## Input Flow

### Joy-Con Button Press → System Action

```
┌────────────────────────────────────────────────────────────────┐
│              Complete Input Flow Diagram                        │
└────────────────────────────────────────────────────────────────┘

PHYSICAL INPUT
──────────────
    👆 User presses ZR trigger on Joy-Con
                    │
                    ▼
        ┌───────────────────────┐
        │  Joy-Con Hardware     │
        │  (Bluetooth Device)   │
        └───────────┬───────────┘
                    │ Bluetooth signal
                    ▼
SYSTEM FRAMEWORK
────────────────
        ┌───────────────────────┐
        │  GameController       │
        │  Framework (macOS)    │
        │                       │
        │ Detects button change │
        └───────────┬───────────┘
                    │ GCController event
                    ▼
APPLICATION LAYER
─────────────────
        ┌───────────────────────────────────────┐
        │  ViewController.swift:handleInput()   │
        │                                       │
        │  if rightTrigger.isPressed {         │
        │    // Check current mode             │
        │    switch currentMode {              │
        │      case .mouse:                    │
        │        inputController.leftClick()   │
        │    }                                 │
        │  }                                   │
        └───────────┬───────────────────────────┘
                    │ Interprets based on mode
                    ▼
CONTROL LOGIC
─────────────
        ┌───────────────────────────────────┐
        │  InputController.leftClick()      │
        │                                   │
        │  1. Get current cursor position   │
        │  2. Create mouseDown event        │
        │  3. Post event to system          │
        │  4. Create mouseUp event          │
        │  5. Post event to system          │
        └───────────┬───────────────────────┘
                    │ CGEvent creation
                    ▼
EVENT CREATION
──────────────
        ┌──────────────────────────────────────┐
        │  CGEvent.mouseEventSource            │
        │                                      │
        │  let downEvent = CGEvent(            │
        │    mouseEventSource: nil,            │
        │    mouseType: .leftMouseDown,        │
        │    mouseCursorPosition: (x, y),      │
        │    mouseButton: .left                │
        │  )                                   │
        │                                      │
        │  downEvent.post(tap: .cghidEventTap) │
        └───────────┬──────────────────────────┘
                    │ Posted to event tap
                    ▼
SYSTEM EVENT TAP
────────────────
        ┌───────────────────────────────┐
        │  macOS Event System           │
        │  (.cghidEventTap)             │
        │                               │
        │  Injects event as if it came  │
        │  from real mouse hardware     │
        └───────────┬───────────────────┘
                    │ Delivered to app
                    ▼
TARGET APPLICATION
──────────────────
        ┌───────────────────────────────┐
        │  Any macOS Application        │
        │  (Chrome, VS Code, etc.)      │
        │                               │
        │  Receives: Mouse click at     │
        │           position (x, y)     │
        │                               │
        │  👆 Button clicked!           │
        └───────────────────────────────┘

TOTAL LATENCY: ~10-30ms from physical press to app receiving event
```

### Analog Stick → Mouse Movement

```
┌────────────────────────────────────────────────────────────┐
│         Continuous Mouse Movement Flow                      │
└────────────────────────────────────────────────────────────┘

    Joy-Con Left Stick moved right →
                │
                ▼
    ┌───────────────────────────────┐
    │ GameController Framework      │
    │                               │
    │ leftThumbstick.xAxis.value    │
    │ = 0.75 (range: -1.0 to 1.0)  │
    └───────────┬───────────────────┘
                │ 120 times per second
                ▼
    ┌────────────────────────────────────┐
    │ ViewController reads analog value  │
    │ every frame (~8ms interval)        │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌───────────────────────────────────────────┐
    │ InputController.setMouseDelta(x, y)      │
    │                                           │
    │ 1. Apply deadzone (ignore < 0.15)        │
    │ 2. Calculate magnitude                    │
    │ 3. Apply acceleration curve (x^1.5)       │
    │ 4. Apply sensitivity (x15)                │
    │ 5. Store in currentMouseDelta             │
    └───────────┬───────────────────────────────┘
                │
                ▼
    ┌───────────────────────────────────────────┐
    │ Timer fires (120 Hz / every 8.3ms)       │
    │                                           │
    │ InputController.updateMouse()             │
    │   if currentMouseDelta != 0:              │
    │     moveMouse(deltaX, deltaY)             │
    └───────────┬───────────────────────────────┘
                │
                ▼
    ┌───────────────────────────────────────────┐
    │ Get current position: (500, 300)         │
    │ Add delta: (500 + 11, 300 + 0)           │
    │ New position: (511, 300)                  │
    │                                           │
    │ CGEvent(.mouseMoved)                      │
    │   .post(tap: .cghidEventTap)             │
    └───────────┬───────────────────────────────┘
                │
                ▼
    ┌───────────────────────────────────────────┐
    │ macOS moves cursor to (511, 300)         │
    │                                           │
    │ 👆 Cursor moves smoothly right →         │
    └───────────────────────────────────────────┘

The stick value is read continuously at 120Hz, creating smooth motion
```

---

## Control Modes

The app has 3 different control modes that change how Joy-Con inputs are interpreted:

```
┌──────────────────────────────────────────────────────────────┐
│                     Control Modes                             │
└──────────────────────────────────────────────────────────────┘

Mode 1: 🖱️  MOUSE MODE (Default)
────────────────────────────────────
┌─────────────────────────────────────────────────────────────┐
│ Joy-Con Input        →    System Action                     │
├─────────────────────────────────────────────────────────────┤
│ Left Stick           →    Move cursor                       │
│ ZR Trigger           →    Left click                        │
│ ZL Trigger           →    Right click                       │
│ R Bumper             →    Modifier: Shift                   │
│ L Bumper             →    Modifier: Option                  │
│ ZL (hold)            →    Modifier: Command                 │
│ L + R                →    Modifier: Control                 │
│ Right Stick Press    →    Toggle precision mode             │
│ A Button             →    Enter                             │
│ B Button             →    Escape                            │
│ X Button             →    Tab                               │
│ Y Button             →    Space                             │
│ D-Pad Up/Down        →    Scroll up/down                    │
│ D-Pad Left/Right     →    Page up/down                      │
└─────────────────────────────────────────────────────────────┘

Example: ZL (hold) + ZR = Cmd+Click (open link in new tab)


Mode 2: 📜 SCROLL MODE
──────────────────────
┌─────────────────────────────────────────────────────────────┐
│ Joy-Con Input        →    System Action                     │
├─────────────────────────────────────────────────────────────┤
│ Left Stick           →    Scroll vertically/horizontally    │
│ Right Stick          →    Smooth scroll                     │
│ ZR/ZL                →    Page down/up                      │
│ D-Pad                →    Arrow keys                        │
│                                                              │
│ Optimized for: Terminal output, long documents, logs        │
└─────────────────────────────────────────────────────────────┘


Mode 3: ⌨️  TEXT MODE
─────────────────────
┌─────────────────────────────────────────────────────────────┐
│ Joy-Con Input        →    System Action                     │
├─────────────────────────────────────────────────────────────┤
│ D-Pad                →    Arrow keys (navigate text)        │
│ A Button             →    Enter                             │
│ B Button             →    Backspace                         │
│ X Button             →    Tab (autocomplete)                │
│ Y Button             →    Space                             │
│ ZL + ZR              →    Hold for voice input              │
│                                                              │
│ Optimized for: Text editing, terminal commands, coding      │
└─────────────────────────────────────────────────────────────┘

Switch modes: Press + Button (or minus button)
```

### Modifier State System

```
┌──────────────────────────────────────────────────────────────┐
│            Modifier Keys Implementation                       │
└──────────────────────────────────────────────────────────────┘

ModifierState struct tracks which modifier keys are active:
──────────────────────────────────────────────────────────────

    struct ModifierState {
        var command: Bool = false    // ⌘ (ZL held)
        var option: Bool = false     // ⌥ (L bumper held)
        var shift: Bool = false      // ⇧ (R bumper held)
        var control: Bool = false    // ⌃ (L + R held)
    }

Example combinations:
─────────────────────

    ZR alone           →  Click
    L + ZR             →  Option+Click
    R + ZR             →  Shift+Click
    ZL + ZR            →  Cmd+Click
    L + R + ZR         →  Ctrl+Click

The modifiers are applied when creating CGEvents:
─────────────────────────────────────────────────

    func pressKey(_ keyCode: CGKeyCode,
                  modifiers: ModifierState) {

        var flags: CGEventFlags = []
        if modifiers.command { flags.insert(.maskCommand) }
        if modifiers.option  { flags.insert(.maskAlternate) }
        if modifiers.shift   { flags.insert(.maskShift) }
        if modifiers.control { flags.insert(.maskControl) }

        event.flags = flags  // ← macOS sees Cmd+C, etc.
    }
```

---

## Voice Input System

```
┌──────────────────────────────────────────────────────────────┐
│              Voice Input Flow                                 │
└──────────────────────────────────────────────────────────────┘

Step 1: Activation
──────────────────
    User holds ZL + ZR buttons together
                │
                ▼
    ┌───────────────────────────────┐
    │ ViewController detects combo  │
    │                               │
    │ voiceManager.startListening() │
    └───────────┬───────────────────┘
                │
                ▼

Step 2: Start Recording
───────────────────────
    ┌──────────────────────────────────────┐
    │ VoiceInputManager.startListening()   │
    │                                      │
    │ 1. Check microphone permission       │
    │ 2. Create SFSpeechRecognizer         │
    │ 3. Create audio engine               │
    │ 4. Start recording from microphone   │
    └───────────┬──────────────────────────┘
                │
                ▼
    ┌──────────────────────────────────────┐
    │  Microphone captures audio           │
    │  ┌────────────────────────┐         │
    │  │  🎤 Audio samples       │         │
    │  └────────────────────────┘         │
    └───────────┬──────────────────────────┘
                │ Audio buffer
                ▼

Step 3: Real-time Recognition
──────────────────────────────
    ┌──────────────────────────────────────┐
    │  SFSpeechRecognizer (Apple's API)    │
    │                                      │
    │  Processes audio in real-time        │
    │  Uses on-device or cloud ML          │
    └───────────┬──────────────────────────┘
                │
                ├─── Interim results ───┐
                │                        │
                ▼                        ▼
    ┌─────────────────────┐   ┌──────────────────┐
    │ Partial transcript  │   │ Status overlay   │
    │ "hello wo..."       │──▶│ shows live text  │
    └─────────────────────┘   └──────────────────┘
                │
                ▼
    ┌─────────────────────┐
    │ Final transcript    │
    │ "hello world"       │
    └───────────┬─────────┘
                │
                ▼

Step 4: Command Processing
───────────────────────────
    ┌──────────────────────────────────────────┐
    │ VoiceInputManager.processCommand()       │
    │                                          │
    │ Check if transcript is a command:        │
    │                                          │
    │ switch transcript {                      │
    │   case "click":                          │
    │     inputController.leftClick()          │
    │   case "enter":                          │
    │     inputController.pressEnter()         │
    │   case "type ...":                       │
    │     inputController.typeText(text)       │
    │   default:                               │
    │     // Just type what was said           │
    │     inputController.typeText(transcript) │
    │ }                                        │
    └───────────┬─────────────────────────────┘
                │
                ▼
    ┌──────────────────────────────────────────┐
    │ CGEvent keyboard events posted           │
    │                                          │
    │ "h" → "e" → "l" → "l" → "o"            │
    └───────────┬─────────────────────────────┘
                │
                ▼
    ┌──────────────────────────────────────────┐
    │ Active application receives typed text   │
    │                                          │
    │ Terminal, browser, text editor, etc.     │
    │ sees: "hello world"                      │
    └──────────────────────────────────────────┘

Step 5: Deactivation
────────────────────
    User releases ZL + ZR
                │
                ▼
    voiceManager.stopListening()
                │
                ▼
    Audio engine stops, resources released
```

### Voice Commands Supported

```
┌──────────────────────────────────────────────────────────────┐
│                  Voice Command Examples                       │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  "click"           →  inputController.leftClick()            │
│  "right click"     →  inputController.rightClick()           │
│  "enter"           →  inputController.pressEnter()           │
│  "escape"          →  inputController.pressEscape()          │
│  "tab"             →  inputController.pressTab()             │
│  "backspace"       →  inputController.pressBackspace()       │
│  "space"           →  inputController.pressSpace()           │
│                                                               │
│  Anything else is typed literally:                           │
│  "hello world"     →  Types: h-e-l-l-o-space-w-o-r-l-d      │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## Security & Privacy Summary

```
┌──────────────────────────────────────────────────────────────┐
│         How berrry-joyful Protects Your Privacy              │
└──────────────────────────────────────────────────────────────┘

✓ All processing happens locally on your Mac
  - No cloud services for input control
  - No data sent to external servers

✓ Speech recognition uses Apple's SFSpeechRecognizer
  - Can use on-device recognition (private)
  - Optional cloud recognition (your choice)
  - Follows Apple's privacy policies

✓ Accessibility permissions are only used for:
  - Creating mouse/keyboard events
  - NOT used to read screen content
  - NOT used to spy on other apps

✓ Open source code
  - You can audit exactly what the app does
  - No hidden behavior

⚠️  The app CAN control other apps
  - This is the intended functionality
  - Requires explicit user permission
  - User must enable in System Settings
  - Can be revoked at any time
```

---

## Technical Details

### CGEvent Posting Mechanism

```
The core of controlling other apps:
───────────────────────────────────

CGEvent is macOS's event system. Events flow:

    Hardware → IOKit → WindowServer → CGEvent → App

berrry-joyful creates events at the CGEvent level:

    Joy-Con → berrry-joyful → CGEvent → Any App

Using .cghidEventTap means events are injected at the
"HID" (Human Interface Device) level, making them
indistinguishable from real hardware events.

This requires Accessibility permission because it's
powerful enough to:
- Control any app
- Simulate any keyboard shortcut
- Move mouse anywhere
- Click on anything

It CANNOT:
- Read what's on screen (no screen capture)
- Read what you type in other apps
- Access files or data from other apps
```

### Why This Approach?

```
Alternative approaches that DON'T work:
───────────────────────────────────────

❌ AppleScript: Too slow, doesn't work in all apps
❌ UI Automation: Limited, app-specific, requires cooperation
❌ Direct window injection: Sandboxed, blocked by macOS
❌ Kernel extensions: Requires disabling SIP, security risk

✓ CGEvent + Accessibility:
  - Fast (sub-millisecond latency)
  - Universal (works in any app)
  - Secure (requires permission)
  - Supported by Apple
```

